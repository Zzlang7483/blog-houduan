<?xml version="1.0" encoding="UTF-8" ?>
<!--MyBatis配置文件-->
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Config 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.blog.mapper.ArticleMapper">
    <select id="hotArticles"  parameterType="int" resultType="com.blog.dao.Article">
        select id , title from ms_article order by view_counts desc limit #{limit}
    </select>

    <select id="newArticle" parameterType="int" resultType="com.blog.dao.Article">
        select id,title from ms_article order by create_date limit #{limit}
    </select>

    <select id="listArchives" resultType="com.blog.dao.Archives">
        select year(FROM_UNIXTIME(create_date/1000)) year,month(FROM_UNIXTIME(create_date/1000)) month,
            count(*) count from ms_article group by year,month;
    </select>
</mapper>